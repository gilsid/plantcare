// Basic smoke test for Plant Care Tracker app
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('Plant Care Tracker smoke test', () {
    // Basic sanity check that the test framework works
    expect(1 + 1, 2);
  });
}
